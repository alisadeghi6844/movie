import React, { useRef, useState } from 'react';
import HeroSliderOrg from "@/app/structure/organism/slider/HeroSlider"


const HeroSlider = () => {

    return (
        <div>
            <HeroSliderOrg/>
        </div>
    );
};

export default HeroSlider;
